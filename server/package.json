{
  "name": "arabic-codenames-server",
  "version": "1.0.0",
  "private": true,
  "description": "Authoritative Socket.IO server for the Arabic Codenames game",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test --test-concurrency=1 test/*.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.1"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
