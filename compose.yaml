services:
  game-server:
    build:
      context: ./server
    environment:
      HOST: 0.0.0.0
      PORT: 3001
      CORS_ORIGIN: http://localhost:8081
    ports:
      - "3001:3001"
    restart: unless-stopped
